// const Discord = require('discord.js')

// exports.run = async(client, message, args) => {


           // let data = await fetch('https://nekos.life/api/v2/img/hug');
           // let response = JSON.parse(await data.text());
          //  let embed = new Discord.RichEmbed()
           // .setTitle(`**${message.author.username}** está abraçando **${member.user.username}**`)
            //.setColor('#dd67ff')
            //.setTimestamp()
           // .setImage(response.url)
           // message.channel.send({embed});  

//exports.help = {
   // Nome: "hug",
   // ExemploTitle: 'Hug em inglês significa abraçar! Uau!',
    //Descrição: "Vai abraçar uma pessoa!",
    //ExemploDesc: 'Abraçar uma pessoa é um gesto muito gentil quando ela tá mal.',
    //Uso: "hug [usuario]",
    //ExemploUso: 'EX: dy!hug @Struater'

//}
